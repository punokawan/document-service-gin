package folders

import "document-service-gin/models"

// Import the documentModel from the models
var folderModel = new(models.FolderModel)

// FolderController defines the document controller methods
type FolderController struct{}