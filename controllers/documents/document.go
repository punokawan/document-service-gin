package documents

import (
	"document-service-gin/models"
)

// Import the documentModel from the models
var documentModel = new(models.DocumentModel)
var folderModel = new(models.FolderModel)

// DocumentController defines the document controller methods
type DocumentController struct {}

